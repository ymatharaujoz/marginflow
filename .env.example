# Public app metadata
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_API_BASE_URL=
# Web only (never in production): auth/billing fictícios no servidor + dados mock no painel sem API.
NEXT_PUBLIC_USE_MOCK_DATA=
NEXT_PUBLIC_WHATSAPP_DEMO_URL=
WEB_APP_ORIGIN=
API_HOST=
API_PORT=
NGROK_AUTHTOKEN=
NGROK_DOMAIN=

# Server-only environment
# Point DATABASE_URL at Supabase pooled/runtime connection string.
# Point DATABASE_MIGRATION_URL at Supabase direct or migration-safe connection string.
DATABASE_URL=
DATABASE_MIGRATION_URL=
BETTER_AUTH_URL=
BETTER_AUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_ANNUAL=

# API only: when true (never applied if NODE_ENV=production), skips overnight + “window already used” sync rules for local integration testing.
# SYNC_RELAX_GUARDS=true

# Optional and reserved for later Supabase app/service integrations.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
